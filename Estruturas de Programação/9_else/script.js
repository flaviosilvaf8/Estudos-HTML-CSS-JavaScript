let nome = 'Flavio'; //Guardara o nome Flavio na variavel nome
if(nome == 'Flavio') {
    console.log('Bem-vindo sr Flavio'); // Se o nome for igual a Flavio, imprimira a mensagem no console.
} else{
    console.log('Você não é o Flavio');// Senão imprimirá que não é o Flavio.
}

let senha = prompt('Digite a sua senha: '); //Aparecerá um prompt com a mensagem setada.
if(senha == 1234){ 
    alert('Senha correta');//se a senha for 1234, aparecera que a senha esta correta
} else{
    alert('A senha esta incorreta');// senão aparecera que a senha esta incorreta.
}

