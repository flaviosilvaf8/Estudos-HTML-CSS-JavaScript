var nome = "Flavio" // variavel nome recebeu "Flavio"

const ip = "10.174.74.03"; // variavel constante recebeu um numero de IP (essa variavel declarada como "const" não poderá ser alterada no decorrer do programa.)

console.log(nome); // imprimira no console o nome declarado

console.log(ip);// imprimira no console o numero de ip declarado
