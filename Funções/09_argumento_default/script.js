function multiplicação (a, b = 2) { //b será 2 por padrão
    console.log(a*b); // multiplicara o numero de a vezes b
}

multiplicação(2);  // 2 sera armazenado na variavel a, a variavel b sera 2 pois foi setada como padrão no inicio, caso eu queira, posso mudar o default acrescentando outro numero na function.